<!doctype html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Madhan Ramesh — SRE / DevOps / Observability</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500;600;700&family=IBM+Plex+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
<style>
  :root{
    --bg:#0A0F1A;
    --bg-elev:#0F1729;
    --panel:#131D30;
    --panel-2:#0D1524;
    --border:#22304A;
    --border-soft:#1A2438;
    --text:#E7ECF3;
    --text-dim:#8FA0BB;
    --text-faint:#5C6C87;
    --teal:#2DD4BF;
    --amber:#F2A93B;
    --red:#EF6461;
    --indigo:#8DA6FF;
    --mono:'IBM Plex Mono', ui-monospace, monospace;
    --sans:'IBM Plex Sans', system-ui, sans-serif;
  }
  *{box-sizing:border-box; margin:0; padding:0;}
  html{scroll-behavior:smooth;}
  body{
    background:var(--bg);
    color:var(--text);
    font-family:var(--sans);
    line-height:1.5;
    -webkit-font-smoothing:antialiased;
    background-image:
      radial-gradient(circle at 15% 0%, rgba(141,166,255,0.08), transparent 40%),
      radial-gradient(circle at 85% 15%, rgba(45,212,191,0.06), transparent 45%);
  }
  a{color:inherit;}
  ::selection{background:var(--teal); color:#04120F;}
  :focus-visible{outline:2px solid var(--indigo); outline-offset:3px;}

  .wrap{max-width:1120px; margin:0 auto; padding:0 28px;}

  /* ---------- topbar ---------- */
  header.topbar{
    position:sticky; top:0; z-index:50;
    background:rgba(10,15,26,0.85);
    backdrop-filter:blur(10px);
    border-bottom:1px solid var(--border-soft);
  }
  .topbar-inner{
    max-width:1120px; margin:0 auto; padding:16px 28px;
    display:flex; align-items:center; justify-content:space-between;
    font-family:var(--mono); font-size:13px;
  }
  .logo{font-weight:600; color:var(--text); letter-spacing:0.02em;}
  .logo span{color:var(--teal);}
  nav.toplinks{display:flex; gap:24px; color:var(--text-dim);}
  nav.toplinks a{text-decoration:none; transition:color .15s;}
  nav.toplinks a:hover{color:var(--teal);}
  .clock{color:var(--text-faint); display:flex; align-items:center; gap:8px;}
  .dot-live{width:7px; height:7px; border-radius:50%; background:var(--teal); box-shadow:0 0 0 0 rgba(45,212,191,.6); animation:pulse 2s infinite;}
  @keyframes pulse{
    0%{box-shadow:0 0 0 0 rgba(45,212,191,.5);}
    70%{box-shadow:0 0 0 6px rgba(45,212,191,0);}
    100%{box-shadow:0 0 0 0 rgba(45,212,191,0);}
  }

  /* ---------- hero ---------- */
  .hero{padding:72px 0 40px;}
  .eyebrow{
    font-family:var(--mono); font-size:12px; letter-spacing:.06em;
    color:var(--amber); background:rgba(242,169,59,0.08);
    border:1px solid rgba(242,169,59,0.25); display:inline-flex; align-items:center; gap:8px;
    padding:6px 12px; border-radius:100px; margin-bottom:24px;
  }
  .eyebrow .dot{width:6px; height:6px; border-radius:50%; background:var(--amber); animation:pulse 2s infinite;}
  h1.name{
    font-family:var(--mono); font-weight:600; font-size:clamp(40px,6vw,64px);
    letter-spacing:-0.02em; line-height:1.05; color:var(--text);
  }
  .role-line{
    font-size:clamp(19px,2.4vw,25px); color:var(--indigo); font-weight:500;
    margin-top:14px; max-width:640px;
  }
  .hero-sub{
    color:var(--text-dim); max-width:560px; margin-top:16px; font-size:16px;
  }
  .hero-cta{display:flex; gap:12px; margin-top:32px; flex-wrap:wrap;}
  .btn{
    font-family:var(--mono); font-size:13px; padding:11px 18px; border-radius:6px;
    text-decoration:none; border:1px solid var(--border); transition:all .15s;
    display:inline-flex; align-items:center; gap:8px;
  }
  .btn-primary{background:var(--teal); color:#04120F; border-color:var(--teal); font-weight:600;}
  .btn-primary:hover{background:#3ee0ca;}
  .btn-ghost{color:var(--text-dim); background:transparent;}
  .btn-ghost:hover{border-color:var(--indigo); color:var(--indigo);}

  /* ---------- trace waterfall (signature) ---------- */
  .trace-panel{
    margin-top:56px; background:var(--panel); border:1px solid var(--border);
    border-radius:10px; overflow:hidden;
  }
  .trace-head{
    display:flex; justify-content:space-between; align-items:center;
    padding:12px 18px; border-bottom:1px solid var(--border-soft);
    font-family:var(--mono); font-size:12px; color:var(--text-faint);
  }
  .trace-head b{color:var(--text-dim); font-weight:500;}
  .trace-body{padding:20px 18px 22px;}
  .trace-row{
    display:grid; grid-template-columns:200px 1fr 64px; align-items:center;
    gap:14px; padding:7px 0; font-family:var(--mono); font-size:12.5px;
  }
  .trace-row .svc{color:var(--text-dim); white-space:nowrap; overflow:hidden; text-overflow:ellipsis;}
  .trace-row .svc small{display:block; color:var(--text-faint); font-size:10.5px; margin-top:1px;}
  .trace-track{position:relative; height:16px; background:var(--panel-2); border-radius:3px; overflow:hidden;}
  .trace-bar{
    position:absolute; top:0; left:0; height:100%; width:0%;
    border-radius:3px; background:var(--indigo);
    transition:width 0.6s cubic-bezier(.2,.7,.3,1), background-color .4s;
  }
  .trace-bar.warn{background:var(--amber);}
  .trace-bar.ok{background:var(--teal);}
  .trace-row .dur{text-align:right; color:var(--text-faint);}
  .trace-footer{
    display:flex; gap:18px; padding:12px 18px; border-top:1px solid var(--border-soft);
    font-family:var(--mono); font-size:11.5px; color:var(--text-faint); flex-wrap:wrap;
  }
  .legend{display:flex; align-items:center; gap:6px;}
  .legend i{width:8px; height:8px; border-radius:2px; display:inline-block;}

  @media (max-width:640px){
    .trace-row{grid-template-columns:1fr; gap:4px;}
    .trace-row .dur{text-align:left;}
  }

  /* ---------- stats strip ---------- */
  .stats-strip{
    display:flex; flex-wrap:wrap; gap:10px; padding:40px 0 8px;
  }
  .pill{
    font-family:var(--mono); font-size:12px; color:var(--text-dim);
    background:var(--panel); border:1px solid var(--border);
    padding:8px 14px; border-radius:100px;
  }

  /* ---------- section shell ---------- */
  section{padding:72px 0;}
  .section-tag{
    font-family:var(--mono); font-size:12px; color:var(--teal); letter-spacing:.08em;
    text-transform:uppercase; margin-bottom:10px;
  }
  h2.section-title{
    font-family:var(--mono); font-size:clamp(24px,3.4vw,32px); font-weight:600;
    color:var(--text); margin-bottom:14px; letter-spacing:-0.01em;
  }
  .section-desc{color:var(--text-dim); max-width:600px; margin-bottom:44px; font-size:15px;}

  /* ---------- skills ---------- */
  .skills-grid{display:grid; grid-template-columns:repeat(auto-fit,minmax(280px,1fr)); gap:16px;}
  .svc-card{
    background:var(--panel); border:1px solid var(--border); border-radius:10px; padding:20px 20px 18px;
  }
  .svc-card-head{display:flex; align-items:center; justify-content:space-between; margin-bottom:14px;}
  .svc-name{font-family:var(--mono); font-size:13.5px; font-weight:600; color:var(--text);}
  .svc-status{font-family:var(--mono); font-size:10px; color:var(--teal); background:rgba(45,212,191,0.08); border:1px solid rgba(45,212,191,0.25); padding:2px 8px; border-radius:100px;}
  .tag-row{display:flex; flex-wrap:wrap; gap:6px;}
  .tag{
    font-size:12px; color:var(--text-dim); background:var(--panel-2);
    border:1px solid var(--border-soft); padding:4px 9px; border-radius:5px;
  }

  /* ---------- projects ---------- */
  .flagship{
    background:linear-gradient(180deg, var(--panel), var(--panel-2));
    border:1px solid var(--border); border-radius:12px; padding:32px; margin-bottom:20px;
  }
  .flagship-top{display:flex; justify-content:space-between; align-items:flex-start; gap:20px; flex-wrap:wrap;}
  .flagship-badge{font-family:var(--mono); font-size:11px; color:var(--amber); border:1px solid rgba(242,169,59,.3); background:rgba(242,169,59,.08); padding:3px 10px; border-radius:100px; margin-bottom:12px; display:inline-block;}
  .flagship h3{font-family:var(--mono); font-size:22px; color:var(--text); margin-bottom:8px;}
  .flagship p.desc{color:var(--text-dim); max-width:640px; margin-bottom:18px; font-size:14.5px;}
  .badges{display:flex; gap:8px; flex-wrap:wrap;}
  .badge{font-family:var(--mono); font-size:11px; color:var(--text-dim); border:1px solid var(--border-soft); padding:3px 9px; border-radius:5px;}
  .flagship-grid{display:grid; grid-template-columns:1.1fr 0.9fr; gap:28px; margin-top:24px;}
  .arch-list{list-style:none; display:flex; flex-direction:column; gap:10px;}
  .arch-list li{font-size:13.5px; color:var(--text-dim); padding-left:18px; position:relative;}
  .arch-list li::before{content:'▸'; position:absolute; left:0; color:var(--teal);}
  .metric-panel{background:var(--panel-2); border:1px solid var(--border-soft); border-radius:8px; padding:16px;}
  .metric-head{display:flex; justify-content:space-between; font-family:var(--mono); font-size:11px; color:var(--text-faint); margin-bottom:10px;}
  .metric-head b{color:var(--text-dim);}
  svg.spark{width:100%; height:70px; display:block;}
  .flagship-links{margin-top:22px; display:flex; gap:12px;}

  @media (max-width:800px){ .flagship-grid{grid-template-columns:1fr;} }

  .proj-grid{display:grid; grid-template-columns:repeat(auto-fit,minmax(300px,1fr)); gap:16px;}
  .proj-card{
    background:var(--panel); border:1px solid var(--border); border-radius:10px; padding:22px;
    display:flex; flex-direction:column; gap:12px;
  }
  .proj-card-top{display:flex; justify-content:space-between; align-items:flex-start; gap:10px;}
  .proj-card h4{font-family:var(--mono); font-size:15px; color:var(--text);}
  .proj-status{font-family:var(--mono); font-size:9.5px; padding:2px 8px; border-radius:100px; white-space:nowrap;}
  .proj-status.public{color:var(--teal); background:rgba(45,212,191,.08); border:1px solid rgba(45,212,191,.25);}
  .proj-status.private{color:var(--text-faint); background:var(--panel-2); border:1px solid var(--border-soft);}
  .proj-card p{font-size:13.5px; color:var(--text-dim);}
  .proj-tags{display:flex; flex-wrap:wrap; gap:6px; margin-top:auto;}
  .proj-link{font-family:var(--mono); font-size:12px; color:var(--indigo); text-decoration:none;}
  .proj-link:hover{text-decoration:underline;}
  .more-note{margin-top:26px; font-family:var(--mono); font-size:12.5px; color:var(--text-faint); text-align:center;}

  /* ---------- contact / status footer ---------- */
  .status-footer{
    border-top:1px solid var(--border-soft); background:var(--bg-elev);
  }
  .status-head{display:flex; align-items:center; gap:10px; margin-bottom:8px;}
  .status-head .big-dot{width:10px; height:10px; border-radius:50%; background:var(--teal); animation:pulse 2s infinite;}
  .status-head span{font-family:var(--mono); font-size:14px; color:var(--teal); font-weight:600;}
  .contact-grid{
    display:grid; grid-template-columns:repeat(auto-fit,minmax(220px,1fr)); gap:20px; margin-top:36px;
  }
  .contact-item{border-top:1px solid var(--border-soft); padding-top:14px;}
  .contact-item .k{font-family:var(--mono); font-size:11px; color:var(--text-faint); text-transform:uppercase; letter-spacing:.05em; margin-bottom:6px;}
  .contact-item a, .contact-item span.v{font-size:14.5px; color:var(--text);}
  .contact-item a{text-decoration:none; border-bottom:1px solid var(--border);}
  .contact-item a:hover{color:var(--teal); border-color:var(--teal);}
  footer.foot{padding:28px 0 40px; font-family:var(--mono); font-size:11.5px; color:var(--text-faint); text-align:center;}

  @media (prefers-reduced-motion: reduce){
    *{animation-duration:0.01ms !important; transition-duration:0.01ms !important;}
  }
</style>
</head>
<body>

<header class="topbar">
  <div class="topbar-inner">
    <div class="logo">madhan<span>.ramesh</span></div>
    <nav class="toplinks">
      <a href="#skills">skills</a>
      <a href="#projects">projects</a>
      <a href="#contact">contact</a>
    </nav>
    <div class="clock"><span class="dot-live"></span><span id="clockText">Reutlingen, DE</span></div>
  </div>
</header>

<main class="wrap">

  <section class="hero">
    <div class="eyebrow"><span class="dot"></span>STATUS · final phase of M.Sc. thesis · open to SRE / DevOps / Observability roles</div>
    <h1 class="name">Madhan Ramesh</h1>
    <p class="role-line">I build the pipelines that tell you something's wrong — before your users notice.</p>
    <p class="hero-sub">Site Reliability / DevOps engineer focused on observability: OpenTelemetry, Prometheus, Grafana, and the Kubernetes plumbing that keeps distributed systems honest. Based in Reutlingen, Germany — open to relocate or remote.</p>
    <div class="hero-cta">
      <a class="btn btn-primary" href="mailto:madhanramesh12233@gmail.com">Email me</a>
      <a class="btn btn-ghost" href="https://github.com/madhan0208" target="_blank" rel="noopener">GitHub ↗</a>
      <a class="btn btn-ghost" href="https://linkedin.com/in/madhan-ramesh" target="_blank" rel="noopener">LinkedIn ↗</a>
    </div>

    <div class="trace-panel">
      <div class="trace-head">
        <div><b>trace_id:</b> 7f3a9c… <b style="margin-left:14px;">request:</b> GET /checkout</div>
        <div id="traceStatus">collecting spans…</div>
      </div>
      <div class="trace-body" id="traceBody"></div>
      <div class="trace-footer">
        <div class="legend"><i style="background:var(--indigo)"></i> in progress</div>
        <div class="legend"><i style="background:var(--teal)"></i> healthy</div>
        <div class="legend"><i style="background:var(--amber)"></i> latency spike</div>
        <div style="margin-left:auto;">simulated trace, based on the pipeline in otel-heterogeneous-reference</div>
      </div>
    </div>
  </section>

  <div class="stats-strip">
    <div class="pill">📍 Reutlingen, DE — open to relocate / remote</div>
    <div class="pill">3 public repos · MIT licensed</div>
    <div class="pill">AKS · Prometheus · Grafana · Loki · Jaeger</div>
    <div class="pill">M.Sc. — final thesis phase</div>
  </div>

  <section id="skills">
    <div class="section-tag">// service catalog</div>
    <h2 class="section-title">What I run — in production and in my homelab</h2>
    <p class="section-desc">Grouped the way I'd group a service map: by what each layer is responsible for.</p>

    <div class="skills-grid" id="skillsGrid"></div>
  </section>

  <section id="projects">
    <div class="section-tag">// deployed & building</div>
    <h2 class="section-title">Projects</h2>
    <p class="section-desc">The flagship is a full observability pipeline I built end to end. Below it, smaller projects across cloud, IoT, and applied ML.</p>

    <div class="flagship">
      <div class="flagship-top">
        <div>
          <div class="flagship-badge">FLAGSHIP · MIT LICENSED · PUBLIC</div>
          <h3>otel-heterogeneous-reference</h3>
          <p class="desc">A reference implementation of OpenTelemetry across a heterogeneous, multi-language stack — built to prove out a two-tier collector pipeline and SLO-based alerting the way I'd want it done in production.</p>
          <div class="badges">
            <span class="badge">.NET + Go</span>
            <span class="badge">Kubernetes</span>
            <span class="badge">Prometheus / Grafana</span>
            <span class="badge">Loki + Jaeger</span>
            <span class="badge">Helm / Docker</span>
            <span class="badge">GitHub Actions + Azure Pipelines</span>
          </div>
        </div>
      </div>

      <div class="flagship-grid">
        <ul class="arch-list">
          <li>Two-tier OTel Collector pipeline — DaemonSet agent feeding a central gateway</li>
          <li>W3C trace context propagation across .NET and Go services</li>
          <li>RED metrics + SLO burn-rate alerting defined and wired in Grafana</li>
          <li>Chaos engineering: pod-kill drills with documented post-mortems — ~30s self-healing validated</li>
          <li>ADR log + a written Telemetry Minimum Standard for the repo</li>
          <li>CI/CD via GitHub Actions and Azure Pipelines, images pushed to Azure Container Registry</li>
        </ul>
        <div class="metric-panel">
          <div class="metric-head"><b>order-service</b> · p95 latency (demo)</div>
          <svg class="spark" viewBox="0 0 300 70" preserveAspectRatio="none">
            <polyline id="sparkLine" fill="none" stroke="#2DD4BF" stroke-width="2" points=""/>
          </svg>
          <div class="metric-head" style="margin-top:8px;"><span>burn rate</span><b id="burnRateVal">0.4x</b></div>
        </div>
      </div>

      <div class="flagship-links">
        <a class="btn btn-primary" href="https://github.com/madhan0208/otel-heterogeneous-reference" target="_blank" rel="noopener">View repo ↗</a>
      </div>
    </div>

    <div class="proj-grid" id="projGrid"></div>

    <div class="more-note">// more projects shipping regularly — this repo list keeps growing</div>
  </section>

  <section id="contact" class="status-footer">
    <div class="wrap" style="padding-top:60px; padding-bottom:20px;">
      <div class="status-head"><span class="big-dot"></span><span>All systems operational — open to new roles</span></div>
      <h2 class="section-title" style="margin-top:18px;">Let's talk</h2>
      <p class="section-desc" style="margin-bottom:0;">I'm finishing my master's thesis and actively looking for SRE, DevOps, or Observability-focused roles. Happy to relocate, and equally happy remote.</p>

      <div class="contact-grid">
        <div class="contact-item"><div class="k">Email</div><a href="mailto:madhanramesh12233@gmail.com">madhanramesh12233@gmail.com</a></div>
        <div class="contact-item"><div class="k">Phone</div><span class="v">+49 152 3623 4011</span></div>
        <div class="contact-item"><div class="k">LinkedIn</div><a href="https://linkedin.com/in/madhan-ramesh" target="_blank" rel="noopener">linkedin.com/in/madhan-ramesh</a></div>
        <div class="contact-item"><div class="k">GitHub</div><a href="https://github.com/madhan0208" target="_blank" rel="noopener">github.com/madhan0208</a></div>
        <div class="contact-item"><div class="k">Location</div><span class="v">72762 Reutlingen, Germany</span></div>
      </div>
    </div>
  </section>

</main>

<footer class="foot">© <span id="year"></span> Madhan Ramesh — built with HTML, CSS, and a live trace simulator instead of a static screenshot.</footer>

<script>
document.getElementById('year').textContent = new Date().getFullYear();

// live-ish clock for Reutlingen (CET/CEST) — uses local browser TZ conversion for Europe/Berlin
function updateClock(){
  try{
    const t = new Intl.DateTimeFormat('en-GB',{hour:'2-digit',minute:'2-digit',timeZone:'Europe/Berlin'}).format(new Date());
    document.getElementById('clockText').textContent = 'Reutlingen, DE · ' + t;
  }catch(e){}
}
updateClock();
setInterval(updateClock, 30000);

// ---------------- trace waterfall ----------------
const spans = [
  {svc:'api-gateway', sub:'ingress', dur:'12ms', width:14, status:'ok'},
  {svc:'auth-service', sub:'Keycloak · OIDC', dur:'28ms', width:22, status:'ok'},
  {svc:'order-service', sub:'.NET', dur:'96ms', width:62, status:'warn'},
  {svc:'inventory-service', sub:'Go', dur:'41ms', width:34, status:'ok'},
  {svc:'otel-collector (agent)', sub:'DaemonSet', dur:'6ms', width:9, status:'ok'},
  {svc:'otel-collector (gateway)', sub:'central pipeline', dur:'14ms', width:16, status:'ok'},
  {svc:'prometheus', sub:'RED metrics', dur:'—', width:8, status:'ok'},
  {svc:'grafana', sub:'SLO burn-rate alert', dur:'—', width:8, status:'ok'},
];

const traceBody = document.getElementById('traceBody');
const traceStatus = document.getElementById('traceStatus');
spans.forEach((s, i) => {
  const row = document.createElement('div');
  row.className = 'trace-row';
  row.innerHTML = `
    <div class="svc">${s.svc}<small>${s.sub}</small></div>
    <div class="trace-track"><div class="trace-bar" id="bar${i}"></div></div>
    <div class="dur" id="dur${i}"></div>
  `;
  traceBody.appendChild(row);
});

function runTrace(){
  traceStatus.textContent = 'collecting spans…';
  spans.forEach((s,i)=>{
    const bar = document.getElementById('bar'+i);
    const dur = document.getElementById('dur'+i);
    bar.style.width = '0%';
    bar.classList.remove('warn','ok');
    dur.textContent = '';
  });
  spans.forEach((s, i) => {
    setTimeout(() => {
      const bar = document.getElementById('bar'+i);
      const dur = document.getElementById('dur'+i);
      bar.style.width = s.width + '%';
      bar.classList.add(s.status === 'warn' ? 'warn' : 'ok');
      dur.textContent = s.dur;
      if(s.status === 'warn'){
        traceStatus.textContent = 'burn-rate alert firing on order-service…';
        setTimeout(()=>{
          bar.classList.remove('warn');
          bar.classList.add('ok');
          traceStatus.textContent = 'self-healed — pod restarted, alert resolved';
        }, 1400);
      }
      if(i === spans.length - 1){
        setTimeout(()=>{ traceStatus.textContent = 'trace complete'; }, 500);
      }
    }, i * 260);
  });
}
runTrace();
setInterval(runTrace, 7000);

// ---------------- sparkline (simulated p95 latency) ----------------
const sparkLine = document.getElementById('sparkLine');
const burnRateVal = document.getElementById('burnRateVal');
let sparkData = Array.from({length: 24}, () => 20 + Math.random()*20);
function renderSpark(){
  sparkData.shift();
  const last = sparkData[sparkData.length-1] ?? 25;
  let next = last + (Math.random()-0.5)*14;
  next = Math.max(8, Math.min(62, next));
  sparkData.push(next);
  const w = 300, h = 70, step = w / (sparkData.length - 1);
  const pts = sparkData.map((v,i)=> `${(i*step).toFixed(1)},${(h - v).toFixed(1)}`).join(' ');
  sparkLine.setAttribute('points', pts);
  const burn = (0.3 + Math.random()*0.9).toFixed(2);
  burnRateVal.textContent = burn + 'x';
  burnRateVal.style.color = burn > 1 ? 'var(--amber)' : 'var(--teal)';
}
renderSpark();
setInterval(renderSpark, 1800);

// ---------------- skills data ----------------
const skillCats = [
  {name:'cloud-containers.svc', tags:['Kubernetes (AKS, prod + personal)','Docker','Helm','ArgoCD','AWS (Lambda, S3, IAM, CloudWatch, Terraform)','Azure (AKS, ACR, App Services, Monitor)','GCP (coursework)']},
  {name:'observability.svc', tags:['OpenTelemetry (SDK & Collector)','OTLP','Prometheus','Grafana','Loki','Jaeger','Elastic APM','SLO / burn-rate alerting','Distributed tracing','Semantic conventions','Elasticsearch (incl. HUGO BOSS collaboration)','InfluxDB','New Relic (familiar)','Splunk (conceptual)']},
  {name:'cicd-automation.svc', tags:['GitHub Actions','GitLab CI/CD','Jenkins','Azure DevOps Pipelines','Terraform','Ansible','Helm','ArgoCD','IaC']},
  {name:'programming.svc', tags:['Python','Go','Bash / Shell','C# / .NET','JavaScript / Node.js','SQL','YAML / JSON','REST APIs']},
  {name:'linux-ops.svc', tags:['Linux administration (Ubuntu, hardening)','RHEL (training-level)','Systemd','Git','Network security fundamentals','Firewall rules','IAM / RBAC']},
  {name:'databases.svc', tags:['MySQL','SQL','MongoDB (Mongoose)','Relational data modeling']},
  {name:'ea-governance.svc', tags:['SAP LeanIX (platform admin, metamodel, automation, dashboards)','SAP ARIS','TOGAF concepts','BPMN','RASCI','ServiceNow CSDM','Architecture Review Boards']},
];
const skillsGrid = document.getElementById('skillsGrid');
skillCats.forEach(cat => {
  const card = document.createElement('div');
  card.className = 'svc-card';
  card.innerHTML = `
    <div class="svc-card-head"><span class="svc-name">${cat.name}</span><span class="svc-status">active</span></div>
    <div class="tag-row">${cat.tags.map(t=>`<span class="tag">${t}</span>`).join('')}</div>
  `;
  skillsGrid.appendChild(card);
});

// ---------------- secondary projects ----------------
const projects = [
  {
    name:'federated-iam-observability-lab', status:'public', badge:'In progress · MIT',
    desc:'AKS lab federating identity (Keycloak, OIDC/SAML) with a FastAPI service instrumented end to end via OTel, feeding Prometheus, Grafana and Azure Monitor.',
    tags:['AKS','Keycloak','FastAPI','OTel','Prometheus','Grafana','Azure Monitor'],
    link:'https://github.com/madhan0208/federated-iam-observability-lab'
  },
  {
    name:'euai', status:'public', badge:'MIT licensed',
    desc:'Full-stack AI app: Vue.js frontend, Node/Express backend, MongoDB. Integrates Google Gemini and OpenAI APIs over WebSocket, with Jest/Supertest coverage.',
    tags:['Vue.js','Node.js / Express','MongoDB','WebSocket','Gemini + OpenAI APIs','Jest / Supertest'],
    link:'https://github.com/madhan0208/euai'
  },
  {
    name:'aws-serverless-cicd', status:'private', badge:'University AWS account',
    desc:'Serverless pipeline on Lambda, S3 and IAM, provisioned with Terraform and deployed via GitHub Actions, with CloudWatch for monitoring.',
    tags:['AWS Lambda','S3','IAM','CloudWatch','Terraform','GitHub Actions'],
    link:null
  },
  {
    name:'IoT weather monitoring', status:'private', badge:'University project',
    desc:'Raspberry Pi sensor network over MQTT, storing time-series data in InfluxDB with Grafana dashboards, plus Telegram/Gmail alerting — a small IT/OT boundary in practice.',
    tags:['Raspberry Pi','MQTT','InfluxDB','Grafana','Python / Bash'],
    link:null
  },
  {
    name:'fake currency detection', status:'private', badge:"Bachelor's thesis, SRM 2022",
    desc:'Image classification pipeline using neural networks, Canny edge detection and GLCM texture features, wrapped in a GUI for demonstration.',
    tags:['Python','Neural Networks','Canny Edge Detection','GLCM'],
    link:null
  },
  {
    name:'university management system', status:'private', badge:'Cognizant internship',
    desc:'A management system built during an internship at Cognizant, covering the full stack from data layer to UI.',
    tags:['ASP.NET','React','MySQL','Docker'],
    link:null
  },
];
const projGrid = document.getElementById('projGrid');
projects.forEach(p => {
  const card = document.createElement('div');
  card.className = 'proj-card';
  card.innerHTML = `
    <div class="proj-card-top">
      <h4>${p.name}</h4>
      <span class="proj-status ${p.status}">${p.badge}</span>
    </div>
    <p>${p.desc}</p>
    <div class="proj-tags">${p.tags.map(t=>`<span class="tag">${t}</span>`).join('')}</div>
    ${p.link ? `<a class="proj-link" href="${p.link}" target="_blank" rel="noopener">View repo ↗</a>` : ''}
  `;
  projGrid.appendChild(card);
});
</script>

</body>
</html>
